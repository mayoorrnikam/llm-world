{
  "name": "llm-world",
  "version": "1.6.0",
  "private": true,
  "description": "An interactive timeline of large language model releases, month by month.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate-data.mjs",
    "validate:links": "node scripts/validate-data.mjs --links",
    "build": "node scripts/build.mjs",
    "build:export": "node scripts/build.mjs --export",
    "check": "npm run validate && npm run build:export && npm run smoke",
    "preview": "npm run build && node scripts/serve.mjs",
    "clean": "rm -rf models companies timeline sitemap.xml robots.txt api llm-releases.csv .build-check",
    "setup": "git config core.hooksPath .githooks && echo 'git hooks enabled \u2014 pre-push now runs npm run check'",
    "smoke": "node scripts/smoke-test.mjs",
    "freshness": "node scripts/check-freshness.mjs"
  }
}
